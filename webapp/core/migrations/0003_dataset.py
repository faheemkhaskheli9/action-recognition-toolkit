# Generated by Django 6.1 on 2026-08-11 17:07

import django.db.models.deletion
from django.db import migrations, models


def create_default_dataset(apps, schema_editor):
    """Before this migration, Dataset didn't exist -- every install used one
    fixed video folder (data/raw) and manifest (data/manifest.csv), only
    varying the manifest path per session. Preserve that as a real Dataset
    row (name="Default") pointing at those exact paths, so existing videos
    and labels keep working without any manual steps."""
    Dataset = apps.get_model("core", "Dataset")
    Dataset.objects.get_or_create(
        slug="default",
        defaults={
            "name": "Default",
            "video_dir": "data/raw",
            "manifest_path": "data/manifest.csv",
        },
    )


def noop(apps, schema_editor):
    pass


class Migration(migrations.Migration):

    dependencies = [
        ("core", "0002_trackextractionrun"),
    ]

    operations = [
        migrations.CreateModel(
            name="Dataset",
            fields=[
                (
                    "id",
                    models.BigAutoField(
                        auto_created=True,
                        primary_key=True,
                        serialize=False,
                        verbose_name="ID",
                    ),
                ),
                ("name", models.CharField(max_length=200, unique=True)),
                ("slug", models.SlugField(max_length=100, unique=True)),
                ("video_dir", models.CharField(max_length=500)),
                ("manifest_path", models.CharField(max_length=500)),
                ("created_at", models.DateTimeField(auto_now_add=True)),
            ],
            options={
                "ordering": ["name"],
            },
        ),
        migrations.AddField(
            model_name="trackextractionrun",
            name="dataset",
            field=models.ForeignKey(
                blank=True,
                null=True,
                on_delete=django.db.models.deletion.SET_NULL,
                related_name="extraction_runs",
                to="core.dataset",
            ),
        ),
        migrations.RunPython(create_default_dataset, noop),
    ]
